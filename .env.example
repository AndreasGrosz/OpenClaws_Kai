# --- OpenClaw Core ---
PORT=3000
OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_TOKEN=<generate with: openssl rand -hex 32>

# --- LLM Provider ---
# Option 1: Anthropic (teuer)
ANTHROPIC_API_KEY=<your-anthropic-api-key>

# Option 2: OpenAI (günstiger)
OPENAI_API_KEY=<your-openai-api-key>

# --- Telegram ---
TELEGRAM_BOT_TOKEN=<your-telegram-bot-token>

# --- Working Directory ---
WORKING_DIRECTORY=/work

# --- Logging ---
LOG_LEVEL=info

# --- Hinweis ---
# Model wird via `openclaw models set <model>` gesetzt, z.B.:
#   openai/gpt-4.1-nano    (sehr günstig)
#   openai/gpt-4.1-mini    (günstig)
#   anthropic/claude-haiku-4-5  (mittel)
#   anthropic/claude-sonnet-4-5 (teuer)
